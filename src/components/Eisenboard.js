import { Grid } from "@material-ui/core";
import React from "react";


function Eisenboard (props) {

    return (
        <Grid container direction="column">
            <Grid item>
                header can contain new task button
            </Grid>
            <Grid item>
                sections
            </Grid>
        </Grid>
    );
}

export default Eisenboard;